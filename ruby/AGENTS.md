# Ruby Guide

General operating principles live in `SKILL.md` at the repository root. Read it first.

## Status

In progress. Chapter files will live directly under `ruby/` as `topic.md` + `topic.sources.md` pairs, matching the convention in `front-end/` and `copywriting/`.

Planned subdomain: `ruby/rails/` for Rails-specific guidelines.

Contributions welcome — see `CONTRIBUTING.md`.
