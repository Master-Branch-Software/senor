---
name: architecture
description: |
  Software architecture patterns, decisions, and trade-off analysis.
  Use this skill for: system design reviews, architectural decision records (ADRs),
  monolith vs. service trade-offs, data modeling, API design, scaling strategies,
  and cross-cutting concerns that span multiple technology stacks.
---

# Architecture Guide

General operating principles live in `SKILL.md` at the repository root. Read it first.

## Status

In progress. Chapter files will live directly under `architecture/` as `topic.md` + `topic.sources.md` pairs, matching the convention in `front-end/` and `copywriting/`.

Contributions welcome — see `CONTRIBUTING.md`.
