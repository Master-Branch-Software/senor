#!/usr/bin/env bash
# check-sources — verify every NN-topic.md has a matching NN-topic.sources.md
# Usage: ./scripts/check-sources [domain]
# Example: ./scripts/check-sources front-end
#          ./scripts/check-sources          (checks all domains)

set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
FAIL=0

check_domain() {
  local domain="$1"
  local refs="$ROOT/$domain/references"

  [[ -d "$refs" ]] || return

  while IFS= read -r -d '' chapter; do
    base="${chapter%.md}"
    sources="${base}.sources.md"
    if [[ ! -f "$sources" ]]; then
      echo "MISSING sources: ${sources#"$ROOT/"}"
      FAIL=1
    fi
  done < <(find "$refs" -maxdepth 1 -name '[0-9][0-9]-*.md' -not -name '*.sources.md' -print0 | sort -z)
}

if [[ $# -ge 1 ]]; then
  check_domain "$1"
else
  for domain in "$ROOT"/*/; do
    check_domain "$(basename "$domain")"
  done
fi

if [[ $FAIL -eq 0 ]]; then
  echo "All chapter files have matching sources files."
else
  echo ""
  echo "Fix: create the missing *.sources.md files per AGENTS.md rule 2."
  exit 1
fi
