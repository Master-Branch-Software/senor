# Hand-formatted HTML — Prettier 3 hardcodes `/>` on void elements,
# which contradicts the chapter 7 rule (and WHATWG: the slash has no
# effect, can interact badly with unquoted attribute values). See
# AGENTS.md and references/16-code-style-and-quality.sources.md.
web-design/index.html

# Eval inputs and recorded LLM outputs — formatting would alter the
# data under test. These are artifacts, not source.
web-design/evals/
web-design-workspace/

# Standard exclusions
node_modules
dist
build
coverage
.next
.nuxt
.svelte-kit
*.min.js
*.min.css
pnpm-lock.yaml
package-lock.json
yarn.lock
bun.lockb
.git
